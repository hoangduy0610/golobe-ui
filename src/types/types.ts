export interface LocationType {
    id: string;  // Thêm id để các phép so sánh trong reducer hoạt động
    name: string;
    address: string;
    description: string;
    featureImage: string;
  }
  